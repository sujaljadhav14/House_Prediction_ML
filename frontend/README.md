# Frontend (Vite + React)

This is a minimal React + Vite frontend that calls the Flask backend to request predictions.

Quick start (Windows cmd):

1. Install dependencies:

    cd frontend
    npm install

2. Run dev server:

    npm run dev

The dev server runs on http://localhost:3000 (by default) and the frontend fetches the API at http://localhost:5000.
